import alt from '../alt';

var AdminActions = {

    approveProblem(index) {
        return index;
    },

    removeProblem(index) {
        return index;
    }

}

module.exports = alt.createActions(AdminActions);